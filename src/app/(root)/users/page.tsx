import React from 'react';
import CombinePage from './combinePage';

export const metadata = {
  title: 'Users | ZKTimey',
  description:
    'View and manage user profiles, roles, departments, and attendance data across your organization.',
};

const page = () => {
  return <CombinePage />;
};

export default page;
