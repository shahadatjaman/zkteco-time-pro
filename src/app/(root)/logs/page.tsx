import React from 'react';
import CombinePage from './combinePage';

export const metadata = {
  title: 'Logs | ZKTimey',
  description:
    'Review and monitor detailed attendance logs, including check-ins, check-outs, device sources, and timestamps for all users.',
};
const Login = () => {
  return <CombinePage />;
};

export default Login;
