import React from 'react';

import RenerPageWithCSR from '.';

export const metadata = {
  title: 'Login | YourApp',
  description: 'Login to access your dashboard, settings, and more.',
};

const Page = () => {
  return <RenerPageWithCSR />;
};

export default Page;
